# CardLink v2.3.2-beta.1 更新说明（草案，未发布）

> **版本**：v2.3.2-beta.1（Beta 预发布通道）｜**适用**：MarginNote 4
> **说明**：本版本整合了 beta.25 → beta.33 开发线的全部成果，并完成发布流程重构（GitHub 发布后自动同步 Gitee 镜像）。

## 📄 PDF 导出重做（本次核心变化）

- 不再依赖系统打印面板：在插件内**本地生成 PDF 字节**并分块写入真实 PDF 文件，完成后直接打开保存面板
- 渲染前等待字体与图片加载完毕、检测页面渲染稳定，避免空白页/半载页
- 图片加载失败时保留原位置并显示占位提示；增加 20 秒渲染超时保护
- 保留"自包含 HTML"保存作为无打印接口设备（部分 iPad 环境）的回退方案

## 🔄 更新通道（OTA）

- 更新检查优先 GitHub；GitHub 不可用时**自动切换 Gitee 备用源**下载同版本安装包

## 📝 错题本与复习

- 错题列表支持**多选、批量改等级、全选/清空、二次确认取消**
- 掌握等级 0–5 级按 Markdown 复习间隔自动排期，支持**自定义复习天数**
- 新增"到期复习"视图：先独立作答、再核对答案并更新掌握等级
- 支持按题名/脑图/章节/分类搜索、按等级筛选
- 性能优化：日常刷新不再全量重扫脑图节点，启用会话缓存，批量操作合并存储

## 🔗 答案匹配与绑定

- 新增**绑定模式**：可为当前题目脑图指定具体答案脑图（关闭时沿用整个学习集）
- 匹配模式支持**标题顺序**与**正则**（题目规则与答案规则可独立配置）
- 绑定记录保存匹配方式与固定卡片 ID，兼容旧绑定

## 📤 导出与兼容

- Markdown 导出为标准 Markdown，不再泄露卡片 HTML/脚本/drawing 原始数据
- 图片扩展名按真实文件头识别；压缩包内 data URI 改写为 assets 路径
- ZIP 导出改用 MN4 全局 ZipArchive

## 🎨 界面与稳定性

- 根页面滚动锁定，仅内容区与专用面板滚动（修复页面错位）
- 详情页 0–5 级等级选择器颜色与设置页统一
- 跨脑图定位改用 MN4 标准卡片链接，为慢设备保留重试窗口并报告超时
- 移除液态玻璃原生调用

## ⚠️ 已知说明

- Beta 通道构建基于运行基线（beta.25）+ 可维护桥接层，源码构建流程待后续对齐（见 `BETA25_BASELINE.md`）
